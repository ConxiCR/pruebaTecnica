//algoritmo que ordene un vector de números de forma descendente
const numeros = [3, 24, 12, 53, 45, 33];
numeros.sort((a, b) => b - a);
//algoritmo que transforme un array de arrays en un array unidimensional
let colores = ['azul', 'rojo', 'amarillo'];
colores.splice(0, 2);

//algoritmo que cuente la cantidad de palabras que contiene una cadena de texto
let cadena = 'Hola que tal';
cadenaCantidad = cadena.length;